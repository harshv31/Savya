# Normalise line endings to LF in the repo, regardless of OS (project built on Windows).
* text=auto eol=lf

# Treat these as binary so git never tries to diff or convert them.
*.png  binary
*.jpg  binary
*.jpeg binary
*.webp binary
*.avif binary
*.ico  binary
*.woff binary
*.woff2 binary
